/* Wikipedia glossary summaries — emptied. Generate glossary terms one at a time with Claude
   (paste the glossary prompt). Add returned entries to glossary.js, or append them here. */
Object.assign(window.GLOSSARY, {});
